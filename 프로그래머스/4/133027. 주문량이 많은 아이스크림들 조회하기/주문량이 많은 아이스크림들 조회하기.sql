SELECT FLAVOR
FROM (
    SELECT A.FLAVOR, SUM(TOTAL_ORDER) TOTAL_ORDER
    FROM (
        SELECT *
        FROM FIRST_HALF
        UNION ALL
        SELECT *
        FROM JULY
    ) A
    GROUP BY A.FLAVOR
) B
ORDER BY TOTAL_ORDER DESC
LIMIT 3;




